{
  "name": "slsmicroservice",
  "dependencies": {
    "@types/node": "^22.12.0",
    "fastify": "^5.2.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "bstart": "rimraf dist && tsc --build && node dist/app.js"
  }
}
